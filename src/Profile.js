const Profile = ({ title }) => {
  return (
    <div className="Profile card">
      <span>
        {title} <span className="card mt">Jesús Mares México,34 años</span>
      </span>
    </div>
  );
};

export default Profile;
