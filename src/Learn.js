const Learn = ({ title }) => {
  return (
    <div className="Learn card">
      <span>
        {title}
        <span className="card mt"> Frontend, REACT</span>
      </span>
    </div>
  );
};

export default Learn;
