const Expectations = ({ title }) => {
  return (
    <div className="Expectations card">
      <span>
        {title} <span className="card mt">Aprender REACT</span>
      </span>
    </div>
  );
};

export default Expectations;
